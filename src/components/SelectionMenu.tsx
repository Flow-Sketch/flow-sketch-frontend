import { css } from '@emotion/react';

export const SelectionMenu = () => {
  return (
    <div
      css={css`
        display: flex;
        position: fixed;
        // 여기에  menu 의 x, y 위치를 계산해야 함.
      `}
    >
      <p></p>
    </div>
  );
};
